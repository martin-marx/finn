prof:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: 'dwh.db'
      schema_directory: '.'
      #optional fields
      extensions:
        - "crypto.dylib"